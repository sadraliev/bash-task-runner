name: Example Workflow
steps:
  - name: Say Hello
    run: echo "Helllo, world!"

  - name: Create a directory and a file
    run: whoami

  - name: List files
    run:
      - echo "Listing files:"
      - ls -la
